name: sandbox-lite

services:
  sandbox:
    build:
      context: .
      args:
        BASE_IMAGE: ${BASE_IMAGE:-nvidia/cuda:13.0.1-runtime-ubuntu24.04}
    image: ${IMAGE_NAME:-sandbox-lite:latest}
    container_name: ${CONTAINER_NAME:-sandbox-lite}
    env_file:
      - path: sandbox.env
        required: false
    environment:
      ASR_HOST: "0.0.0.0"
      INTENT_HOST: "127.0.0.1"
      VIDEO_HOST: "0.0.0.0"
      SANDBOX_USER_PORT: "28502"
      SANDBOX_MANAGEMENT_HOST: "0.0.0.0"
      SANDBOX_MANAGEMENT_PORT: "28501"
      # 9005 only binds to the host loopback interface; it is not an external API.
      SANDBOX_ASR_URL: "http://127.0.0.1:9005/api/v1/transcribe"
      LOG_DIR: "/app/logs"
      LOG_MAX_BYTES: ${LOG_MAX_BYTES:-10485760}
      LOG_BACKUP_COUNT: ${LOG_BACKUP_COUNT:-5}
      VIDEO_PUBLIC_IP: ${VIDEO_PUBLIC_IP:-127.0.0.1}
      VIDEO_WIDTH: ${VIDEO_WIDTH:-640}
      VIDEO_HEIGHT: ${VIDEO_HEIGHT:-480}
      VIDEO_FPS: ${VIDEO_FPS:-30}
      VIDEO_H264_RTP_PAYLOAD_BYTES: ${VIDEO_H264_RTP_PAYLOAD_BYTES:-1150}
    network_mode: host
    volumes:
      - ${ASR_MODEL_SOURCE:-./models/whisper-models/whisper-large-v3}:/models/asr/whisper-large-v3:ro
      - ${INTENT_MODEL_SOURCE:-./models/semantic-models/Qwen/Qwen2.5-0.5B-Instruct}:/models/intent/Qwen2.5-0.5B-Instruct:ro
      - ${YOLO_MODEL_SOURCE:-./models/yolo-models}:/models/yolo:ro
      - ./logs:/app/logs
    gpus: all
    init: true
    restart: unless-stopped
